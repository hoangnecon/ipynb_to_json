{
  "name": "ipynb-to-json",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "node postinstall.js",
    "start": "npm run dev",
    "dev": "concurrently \"npm start --prefix frontend\" \"python -m uvicorn main:app --reload --app-dir backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}